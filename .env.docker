# ==============================================
# 🚀 HeadlessX - Docker Environment Variables
# ==============================================
# Copy this file to .env before running docker-compose
# Example: cp .env.docker .env
# ==============================================

# ----------------------------------------------
# 🗄️ PostgreSQL Database Configuration
# ----------------------------------------------
POSTGRES_USER=headlessx
POSTGRES_PASSWORD=headlessx_secure_password_change_me
POSTGRES_DB=headlessx
POSTGRES_HOST=headlessx-postgres
POSTGRES_PORT=5432

# ----------------------------------------------
# 🔧 Backend Configuration
# ----------------------------------------------
# Backend API Port (exposed to host)
BACKEND_PORT=3001

# Logging
LOG_LEVEL=info

# ----------------------------------------------
# 🎨 Frontend Configuration
# ----------------------------------------------
# Frontend Port (exposed to host)
FRONTEND_PORT=3000

# API URL for browser (external access)
# For local development: http://localhost:3001
# For production: https://api.yourdomain.com
NEXT_PUBLIC_API_URL=http://localhost:3001

# API URL for Next.js server (internal Docker network)
# This is used by Next.js API proxy to communicate with backend
NEXT_PUBLIC_API_URL_INTERNAL=http://backend:3001

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# ==============================================
# 📝 NOTES
# ==============================================
# 
# 1. DATABASE_URL is automatically constructed from POSTGRES_* variables
#    No need to set it manually
#
# 2. Browser & Anti-Detection settings are managed via the Dashboard:
#    - Headless Mode
#    - Browser Timeout
#    - Max Concurrent Jobs
#    - Camoufox Configuration (OS, Humanize, GeoIP)
#    - Proxy Settings
#    - Fingerprint Profiles
#
# 3. For production deployment:
#    - Change POSTGRES_PASSWORD to a strong password
#    - Update NEXT_PUBLIC_API_URL to your domain
#    - Update FRONTEND_URL to your domain
#    - Consider using Docker secrets for sensitive data
#
# 4. Ports:
#    - Frontend: 3000 (customizable via FRONTEND_PORT)
#    - Backend:  3001 (customizable via BACKEND_PORT)
#    - Database: 5432 (customizable via POSTGRES_PORT)
#
# ==============================================
